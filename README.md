# Telegram Transacrition Bot 🎙️

Бот для Telegram, який приймає **аудіо-повідомлення** та **відео у форматі “кружечок”** і робить **транскрипцію у текст**, використовуючи локальну модель Whisper. Підтримується пріоритетна українська мова.

---

## Запуск через Docker

Приклад запуску контейнера:

```bash
docker run -d --name telegram-whisper-bot \
  --env-file .env \
  -v $(pwd)/tmp:/tmp/whisper_bot \
  vadimgurlenko/telegram-whisper-bot:latest
```

---

## Вміст змінних середовищ (.env)

```env
# Telegram
BOT_TOKEN={BOT_TOKEN}  # токен вашого бота

# Whisper settings
WHISPER_MODEL=small       # модель Whisper: tiny, base, small, medium, large
WHISPER_DEVICE=cpu        # пристрій для обчислень: cpu або cuda
WHISPER_COMPUTE_TYPE=int8 # оптимізація моделі для CPU
WHISPER_LANGUAGE=uk       # пріоритетна мова транскрипції

# Paths
TMP_DIR=/tmp/whisper_bot  # директорія для тимчасових файлів
MAX_WORKERS=4             # кількість паралельних транскрипцій
```

